import React from 'react';
import Claims from '../admin/Claims';

/**
 * ManagerClaims — delegates to the shared admin Claims component.
 * Manager role inherits full claim management capabilities (view, edit, approve, reject).
 */
export default Claims;
