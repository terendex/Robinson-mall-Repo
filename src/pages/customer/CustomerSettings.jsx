import React from 'react';
import SettingsPage from '../../components/SettingsPage';
import '../../css/Settings.css';

/**
 * CustomerSettings Component — Customer portal
 * Uses the shared SettingsPage component, consistent with Admin / Manager / Staff.
 */
const CustomerSettings = () => <SettingsPage />;

export default CustomerSettings;
